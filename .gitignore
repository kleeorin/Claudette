node_modules/
dist/
out/
*.log
.DS_Store
*.tsbuildinfo
.claudette/
.claudette-token
# Generated phone-pairing QR — encodes the URL WITH the access token; never commit.
/qr.png
.chrome-headless/

# Local Claude Code session config (per-machine permissions).
.claude/

# The full-run lock (scratchpad/run-suite.sh). In the repo because sessions do NOT share
# /tmp — see that file's header. Never committed.
.suite-run.lock
